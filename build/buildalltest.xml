<project name="Salesforce Deployment" default="retrieveUnpackaged" xmlns:sf="antlib:com.salesforce">

    <property file="build.properties"/>
    <property environment="env"/>

    <condition property="sf.username" value=""> <not> <isset property="sf.username"/> </not> </condition>
    <condition property="sf.sessionId" value=""> <not> <isset property="sf.sessionId"/> </not> </condition>
   
    <taskdef resource="com/salesforce/antlib.xml" uri="antlib:com.salesforce">
 
        <classpath>
            <pathelement location="../ant-salesforce.jar" />        	
        </classpath>
    </taskdef>
    <taskdef resource="net/sf/antcontrib/antcontrib.properties"/>
    <property file="build.properties"/>
    <property environment="env"/>
    
    <target name="retrieveUnpackaged">
        <property name="jwtToken" value="${sf.jwtToken}" />
        <mkdir dir="../src"/>
        <sf:retrieve username="${sf.username}" serverurl="${sf.serverurl}" retrieveTarget="../src/" unpackaged="../manifest/package.xml" jwtToken="${jwtToken}" />
       
        <antcall target="runTests"/>
    </target>

    <target name="runTests">
        <if>
            <available file="../src/classes" type="dir" />
            <then>
              <echo message="Test classes exists running tests..." />
              <sf:deployJWT username="${sf.username}" serverurl="${sf.serverurl}" jwtClientId="${sf.jwtClientId}" jwtPrivateKeyFile="${sf.jwtPrivateKeyFile}" deployroot="../src/" runAllTests="true"/>

            </then>
            <else>
              <echo message="Test Classes does not exist skipping test..." />
              <sf:deployJWT username="${sf.username}" sessionId="${sf.sessionId}" serverurl="${sf.serverurl}" jwtClientId="${sf.jwtClientId}" jwtPrivateKeyFile="${sf.jwtPrivateKeyFile}" maxPoll="${sf.maxPoll}" deployRoot="../src/" rollbackOnError="true"/>
              
            </else>
          </if>
        <antcall target="Clean"/>
    </target>
    <target name="Clean">

        <delete dir="../src"/>
    </target>

</project>
